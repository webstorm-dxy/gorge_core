# 常见问题

# 使用CLion打开一堆红
Xmake编译工具链问题，在windows默认的工具链为vc，使用 `xmake f --toolchain=llvm -c`切换所使用的编译工具链，并重新生成编译数据库

在windows上使用VC++编译工具链可能会出现很多的问题，换用其余编译器时最好进行一些宏处理

# C++模块问题
要在使用模块的代码里使用头文件，要把头文件放到全局模块里面
```c++
module;
//头文件include
#include <fmt/core.h>
module your_module;

...
```
